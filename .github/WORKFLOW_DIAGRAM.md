# GitHub Actions Workflow Diagram

## Workflow Trigger Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     WORKFLOW TRIGGERS                            │
└─────────────────────────────────────────────────────────────────┘
                              │
            ┌─────────────────┼─────────────────┐
            │                 │                 │
            ▼                 ▼                 ▼
      ┌──────────┐     ┌──────────┐     ┌──────────────┐
      │   PUSH   │     │   PULL   │     │   MANUAL     │
      │   main   │     │  REQUEST │     │  TRIGGER     │
      │ develop  │     │          │     │ (Dispatch)   │
      └──────────┘     └──────────┘     └──────────────┘
            │                 │                 │
            └─────────────────┼─────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │   CI WORKFLOW   │
                    │    STARTS       │
                    └─────────────────┘
                              │
                ┌─────────────┼─────────────┐
                │             │             │
                ▼             ▼             ▼
         ┌──────────┐  ┌──────────┐  ┌──────────┐
         │  LINT    │  │   TYPE   │  │  BUILD   │
         │  CODE    │  │  CHECK   │  │   APP    │
         └──────────┘  └──────────┘  └──────────┘
                │             │             │
                └─────────────┼─────────────┘
                              │
                    ┌─────────┴─────────┐
                    │                   │
                    ▼                   ▼
              ┌──────────┐        ┌──────────┐
              │ SUCCESS  │        │  FAILED  │
              │    ✓     │        │    ✗     │
              └──────────┘        └──────────┘
```

## How to Rerun a Workflow

```
┌─────────────────────────────────────────────────────────────────┐
│                    RERUN OPTIONS                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
            ┌─────────────────┼─────────────────┬──────────────┐
            │                 │                 │              │
            ▼                 ▼                 ▼              ▼
   ┌────────────────┐  ┌────────────┐  ┌────────────┐  ┌───────────┐
   │  GitHub UI     │  │   Manual   │  │  GitHub    │  │    Git    │
   │  "Re-run jobs" │  │  Trigger   │  │    CLI     │  │   Push    │
   │                │  │  Button    │  │            │  │           │
   │  ⭐ EASIEST    │  │            │  │            │  │           │
   └────────────────┘  └────────────┘  └────────────┘  └───────────┘
```

## GitHub UI Navigation Flow

```
Repository Homepage
      │
      ▼
┌─────────────┐
│ Actions Tab │ ◄─── CLICK HERE
└─────────────┘
      │
      ├─── Option 1: Rerun Existing
      │    │
      │    ▼
      │    Select Workflow Run
      │    │
      │    ▼
      │    ┌─────────────────────┐
      │    │  "Re-run jobs" ▼    │ ◄─── CLICK DROPDOWN
      │    └─────────────────────┘
      │    │
      │    ├─── "Re-run all jobs"
      │    └─── "Re-run failed jobs"
      │
      └─── Option 2: Manual Trigger
           │
           ▼
           Click "CI" Workflow (left sidebar)
           │
           ▼
           ┌─────────────────────┐
           │  "Run workflow" ▶   │ ◄─── CLICK BUTTON
           └─────────────────────┘
           │
           ▼
           Select Branch → Run
```

## Workflow Status Indicators

```
┌──────────────────────────────────────────────────────────────┐
│  WHERE TO SEE WORKFLOW STATUS                                │
└──────────────────────────────────────────────────────────────┘

1. Actions Tab
   ┌────────────────────────────────────┐
   │ ● Running   (yellow/orange circle) │
   │ ✓ Success   (green checkmark)      │
   │ ✗ Failed    (red X)                │
   │ ○ Queued    (gray circle)          │
   └────────────────────────────────────┘

2. Pull Request
   ┌────────────────────────────────────┐
   │ Checks Section (bottom)            │
   │ ✓ All checks passed                │
   │ ✗ Some checks failed               │
   │ ● Checks running...                │
   └────────────────────────────────────┘

3. Commit List
   ┌────────────────────────────────────┐
   │ abc1234  ✓  Fix bug               │
   │ def5678  ✗  Add feature           │
   │ ghi9012  ●  Update docs           │
   └────────────────────────────────────┘
```

## Quick Access URLs

```
┌──────────────────────────────────────────────────────────────┐
│  BOOKMARK THESE FOR QUICK ACCESS                             │
└──────────────────────────────────────────────────────────────┘

📍 All Workflows:
   https://github.com/mrTsoi/ledgerai/actions

📍 CI Workflow:
   https://github.com/mrTsoi/ledgerai/actions/workflows/ci.yml

📍 Repository Settings (for secrets):
   https://github.com/mrTsoi/ledgerai/settings/secrets/actions
```

## Workflow File Structure

```
.github/
├── workflows/
│   └── ci.yml ◄────────────── Main workflow definition
│
├── WORKFLOW_GUIDE.md ◄────── Comprehensive documentation
├── QUICK_RERUN_GUIDE.md ◄─── Quick reference
└── WORKFLOW_DIAGRAM.md ◄──── This visual guide
```

## Key Features of CI Workflow

```
┌─────────────────────────────────────────────────────────────┐
│  CI WORKFLOW FEATURES                                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ✓  Auto-runs on push to main/develop                      │
│  ✓  Auto-runs on pull requests                             │
│  ✓  Manual trigger support (workflow_dispatch)             │
│  ✓  Node.js 18 with npm caching                            │
│  ✓  ESLint code quality checks                             │
│  ✓  TypeScript type checking                               │
│  ✓  Next.js production build                               │
│  ✓  Environment variable support                           │
│  ✓  Continue-on-error for non-critical steps              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Tips for Success

```
💡 TIP 1: Check workflow logs before rerunning
   Actions Tab → Click workflow run → View logs

💡 TIP 2: Rerun only failed jobs to save time
   "Re-run failed jobs" option in dropdown

💡 TIP 3: Use manual trigger for testing
   No code changes needed, just click "Run workflow"

💡 TIP 4: Add required secrets before first run
   Settings → Secrets → Actions
   - NEXT_PUBLIC_SUPABASE_URL
   - NEXT_PUBLIC_SUPABASE_ANON_KEY

💡 TIP 5: Monitor workflow status in real-time
   Actions tab updates live during execution
```

---

**Need Help?** See [WORKFLOW_GUIDE.md](WORKFLOW_GUIDE.md) for detailed documentation.
